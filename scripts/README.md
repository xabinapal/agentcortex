# Scripts

`scripts/` is reserved for future repository automation.

## Planned subdirectories

- `export/`: map neutral catalog content into native platform layouts
- `validate/`: check directory structure, metadata, and compatibility rules

No automation is implemented yet. These directories exist to make the intended
workflow explicit from the start.
